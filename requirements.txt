# DataForge — Universal Dataset Converter
# Core Dependencies

flask>=3.0.0
flask-cors>=4.0.0
pandas>=2.0.0
pyarrow>=14.0.0
openpyxl>=3.1.0
lxml>=5.0.0
pyyaml>=6.0.0
fastavro>=1.9.0
# tables>=3.9.0  # Optional: requires system HDF5 headers (brew install hdf5)

# Production Server
gunicorn>=21.0.0

# Testing
pytest>=8.0.0
